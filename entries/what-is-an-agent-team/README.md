# What is an agent team?

Foundation article in the Building pillar. Defines an agent team, explains why a single agent cannot carry every job, covers the team pattern (one role, one toolset, one output), walks through three team architectures (sequential, parallel fan-out, supervisor), tackles the handoff problem, and names when a team is the right tool.

~2,300 words. Building pillar. Architecture topic.
